import React from 'react';
import Styled from 'styled-components/native';
import TopNavigator from '~/Components/SearchResult';

const TapNavigatorText = () => {
  return <TopNavigator />;
};

export default TapNavigatorText;
