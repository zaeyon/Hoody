import counterActions from '~/action/counterActions';
import userActions from '~/action/userActions';
import searchAction from '~/action/searchAction';
import feedListAction from '~/action/feedListAction';

const allActions = {
  counterActions,
  userActions,
  searchAction,
  feedListAction,
};

export default allActions;
